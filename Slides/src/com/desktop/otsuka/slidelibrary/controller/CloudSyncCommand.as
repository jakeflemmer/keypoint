package com.desktop.otsuka.slidelibrary.controller
{
	public class CloudSyncCommand
	{
		
		// NOTE this class should exist and be used appropritely
		// however since I have to get ton's done with no time and I can just copy paste quicker for now I am skipping it
		public function CloudSyncCommand()
		{
		}
		
		/*
		public static function makeCustomDeckObject(presentationObject:Object):Object{
			var now:Number = (new Date().time) / 1000;
			
			var customDeck : Object = new Object();
			customDeck.uuid = presentationObject.guid;
			customDeck.deck_name = presentationObject.custom_title;
			customDeck.deck_id = presentationObject.presentation_id;				
			customDeck.updates_last_applied = now;
			customDeck.presenters_name = titleSlideWidget.chosenName;	//"jboogie";
			customDeck.presenters_date = titleSlideWidget.chosenDateString;
			customDeck.user_last_saved = now;
			
			var slideSectionIds:ArrayCollection = makeArrayOfSlideSectionIds();
			customDeck.slide_section_ids = slideSectionIds.source;
			
			return customDeck;
			
		}
		
		public static function makeArrayOfSlideSectionIds 
			makeArrayOfSlideSectionIds
		
		*/
	}
}