package com.desktop.otsuka.slidelibrary.controller
{
	import flash.events.EventDispatcher;
	import flash.events.IEventDispatcher;
	
	public class CustomizeWorkerFunctions extends EventDispatcher
	{
		public function CustomizeWorkerFunctions(target:IEventDispatcher=null)
		{
			super(target);
		}
	}
}